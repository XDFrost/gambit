# No secrets are needed for local development.
# The Worker runs with `wrangler dev` on http://localhost:8787 (Durable Objects emulated locally).
# The Vite dev server on http://localhost:5173 proxies /api and /ws to it.
#
# Deploy: `npx wrangler login` once, then `npm run deploy`.
