# Leave empty for local development (the Vite dev server proxies /api and /ws to the Worker).
# For a Vercel-hosted frontend, set this to your Worker URL, no trailing slash:
# VITE_API_ORIGIN=https://gambit.<your-subdomain>.workers.dev
VITE_API_ORIGIN=
